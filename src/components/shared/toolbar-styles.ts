/** Outline controls in list toolbars (Export, Columns, etc.). */
export const TOOLBAR_OUTLINE_CONTROL_CLASS =
  "h-11 shrink-0 gap-1.5 rounded-[6px] border-border/50 bg-background/80 px-3 text-sm font-medium shadow-subtle hover:border-primary/30";

/** Search field width in list toolbars (matches survey list). */
export const TOOLBAR_SEARCH_WIDTH_CLASS = "w-1/4 max-w-[25%] shrink-0";

/** Search input styling shared across list toolbars. */
export const TOOLBAR_SEARCH_INPUT_CLASS =
  "h-11 w-full rounded-[6px] border-border/50 bg-background/80 pl-9 shadow-subtle";
