/** Outline controls in list toolbars (Export, Columns, etc.). */
export const TOOLBAR_OUTLINE_CONTROL_CLASS =
  "h-9 shrink-0 gap-1.5 rounded-[6px] border-border/50 bg-background/80 px-2.5 text-xs font-medium shadow-subtle hover:border-primary/30 sm:h-10 sm:px-3 sm:text-sm lg:h-11";

/** Search field width — full row on narrow desktop, capped beside filters when space allows. */
export const TOOLBAR_SEARCH_WIDTH_CLASS =
  "min-w-0 w-full flex-1 basis-full sm:min-w-56 sm:basis-56 sm:max-w-xs lg:max-w-sm xl:max-w-md";

/** Search input styling shared across list toolbars. */
export const TOOLBAR_SEARCH_INPUT_CLASS =
  "h-9 w-full rounded-[6px] border-border/50 bg-background/80 pl-8 text-xs shadow-subtle sm:h-10 sm:pl-9 sm:text-sm lg:h-11";
