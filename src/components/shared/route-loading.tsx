/** Route `loading.tsx` stays empty — global API loader owns the only overlay. */
export function RouteLoading(_props?: { label?: string; hint?: string }) {
  return null;
}

export default function DefaultRouteLoading() {
  return null;
}
