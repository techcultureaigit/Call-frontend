"use client";

/**
 * Disabled — NavigationLoader caused a second flash before the API loader.
 * GlobalApiLoader (HTTP interceptors) is the only fullscreen overlay.
 */
export function NavigationLoader() {
  return null;
}
